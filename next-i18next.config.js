/** @type {import('next-i18next').UserConfig} */
const path = require('path')

module.exports = {
    i18n: {
        defaultLocale: 'th',
        locales: ['th' ,'en', 'id'],
        localeDetection: false,
    },
}